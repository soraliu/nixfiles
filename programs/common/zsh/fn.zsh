# -------------------------------------------------------------------------------------------------------------------------------
# functions
# -------------------------------------------------------------------------------------------------------------------------------
zellij-layout-coding() {
  path_to_cwd=${1:-$(pwd)}
  zellij action new-tab --layout coding --name "$(basename ${path_to_cwd})" --cwd="${path_to_cwd}"
}
