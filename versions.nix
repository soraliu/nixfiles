# Centralized version management for nixfiles
# This file defines the main version constants used across the configuration
{
  # NixOS/nixpkgs version - using 25.05 stable release
  # This version is used for nixpkgs, home-manager, and nix-darwin
  # See: https://nixos.org/manual/nixos/stable/release-notes.html
  version = "25.05";
}